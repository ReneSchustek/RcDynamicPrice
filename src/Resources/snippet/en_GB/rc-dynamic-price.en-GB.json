{
    "rc-dynamic-price": {
        "label": "Length in millimetres",
        "placeholder": "e.g. 1500",
        "unit": "mm",
        "calculatedPrice": "Calculated price:",
        "errorIntegerOnly": "Please enter positive whole numbers only.",
        "errorMinLength": "Minimum length: %minLength% mm",
        "errorMaxLength": "Maximum length: %maxLength% mm",
        "errorMaxPieceLength": "Please split the length into pieces of no more than {maxPiece} mm.",
        "roundUpHint": "Input %input% mm → billed as %billed% mm (rounded)",
        "hintModalButton": "Got it",
        "labelWithDetails": "%name% (%details%)",
        "labelLength": "Length %length% mm",
        "labelSplit": "Cut to length %length% mm: %pieces%",
        "labelPiece": "%count%× %length% mm",
        "labelBilled": ", calculated %billed% mm",
        "splitHintEqual": "Your length {length} mm will be split into {pieces} equal pieces of {pieceLength} mm each.",
        "splitHintMaxRest": "Your length {length} mm will be split into full pieces of {maxPiece} mm plus a remainder of {remainder} mm ({pieces} pieces).",
        "splitHintManual": "The entered length {length} mm exceeds the maximum piece length of {maxPiece} mm. Please split the length yourself (e.g. {pieces} × {pieceLength} mm) and add each piece separately to the cart.",
        "minPieceUpliftHint": "The remainder of {remainder} mm is below the minimum length of {minLength} mm. It is cut to that actual length, but charged at the minimum length — {billed} mm in total instead of {input} mm."
    },
    "error": {
        "rc-dynamic-price-meter-price-unavailable": "No price per metre could be determined for %name%. Please remove the item or contact us — ordering is not possible until this is resolved."
    },
    "checkout": {
        "rc-dynamic-price-meter-price-unavailable": "No price per metre could be determined for %name%. Please remove the item or contact us — ordering is not possible until this is resolved."
    }
}
